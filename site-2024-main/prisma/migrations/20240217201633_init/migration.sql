-- CreateTable
CREATE TABLE "Slot" (
    "id" INT4 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    "description" STRING NOT NULL,
    "time" STRING NOT NULL,
    "endTime" STRING NOT NULL,
    "location" STRING NOT NULL,
    "maxParticipants" INT4 NOT NULL,
    "waitListSize" INT4 NOT NULL,

    CONSTRAINT "Slot_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "Volunteer" (
    "id" INT4 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    "clerkId" STRING NOT NULL,
    "email" STRING NOT NULL,
    "name" STRING NOT NULL,

    CONSTRAINT "Volunteer_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "_SlotToVolunteer" (
    "A" INT4 NOT NULL,
    "B" INT4 NOT NULL
);

-- CreateIndex
CREATE UNIQUE INDEX "Volunteer_clerkId_key" ON "Volunteer"("clerkId");

-- CreateIndex
CREATE UNIQUE INDEX "_SlotToVolunteer_AB_unique" ON "_SlotToVolunteer"("A", "B");

-- CreateIndex
CREATE INDEX "_SlotToVolunteer_B_index" ON "_SlotToVolunteer"("B");

-- AddForeignKey
ALTER TABLE "_SlotToVolunteer" ADD CONSTRAINT "_SlotToVolunteer_A_fkey" FOREIGN KEY ("A") REFERENCES "Slot"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "_SlotToVolunteer" ADD CONSTRAINT "_SlotToVolunteer_B_fkey" FOREIGN KEY ("B") REFERENCES "Volunteer"("id") ON DELETE CASCADE ON UPDATE CASCADE;
